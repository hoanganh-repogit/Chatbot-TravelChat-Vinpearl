# LLM providers - server-side only. Do not expose these through VITE_*.
MIMO_API_KEY=
MIMO_BASE_URL=https://token-plan-sgp.xiaomimimo.com/v1
MIMO_MODEL=mimo-v2.5-pro

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

OPENROUTER_API_KEY=
OPENROUTER_MODEL=

# Optional live weather provider. App must fall back to MOOC data when missing.
OPENWEATHER_API_KEY=

# Public frontend envs can use VITE_* only when the key is safe for browsers.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
